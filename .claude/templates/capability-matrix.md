<!--
Capability Matrix template.

Renders a comparison of the project's own current stance against
researched competitors, on the specific capabilities that matter to the
problem/user scope already established in the project's memory store —
not a generic feature checklist. Filled in by `research-agent`
(.claude/agents/research-agent.md); see `.claude/examples/research-agent/`
for a fully worked example.

Placeholders use {{double-brace}} syntax. Every {{evidence: ...}} and
{{record: ...}} placeholder must resolve to a real record id already in
the project's memory store — never a claim invented while rendering.
-->

# Capability Matrix — {{project_slug}}

Generated by `research-agent` on {{generated_at}} from
`.claude/memory/{{project_slug}}.json`.

## Scope

{{one to two sentences stating the specific problem/user scope this
matrix researches — not a broad category — and the record id(s) in
memory that established that scope}}

## Matrix

| Capability | Us (current stance) | {{competitor_1_name}} | {{competitor_2_name}} | {{competitor_3_name}} |
|---|---|---|---|---|
| {{capability}} | {{our stance, citing the `decision`/`insight` record id it comes from; if the strongest record is only an `assumption`, cite it but mark it "(tentative, unvalidated)"; or "Not yet decided" if no record establishes a stance at all}} | {{their stance, citing `research-source` id(s) via the `competitor` record's `evidence_ids`}} | {{...}} | {{...}} |
| {{capability}} | {{...}} | {{...}} | {{...}} | {{...}} |

Add or remove capability rows and competitor columns as needed — this
table is not fixed-width. Every non-"Not yet decided" cell must be
traceable to a record id; a cell with no supporting record shouldn't be
filled in with a guess.

## Why they likely did it that way

For each competitor capability that isn't self-explanatory, one or two
sentences on the plausible reasoning behind their choice — not just
restating the feature. This is normally already captured in the backing
`research-source` record's `details`; pull it forward here rather than
re-deriving it.

- {{competitor_name}} — {{capability}}: {{plausible reason}}

## Sources

- {{research-source id}} — {{data.claim}} (retrieved {{data.retrieved_at}})

## Notes

- This matrix reflects only what's been researched so far — a blank or
  "Not yet decided" cell is a gap to research or decide later, not a "no."
- This matrix does not recommend a direction. Connecting these findings to
  a decision is a separate, deliberate step for a human or a future
  strategy skill — not something `research-agent` does on its own.
